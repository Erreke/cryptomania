export default {
  SET_FIRST_LEVEL_STEPS(state, steps) {
    state.firstLevelSteps = steps;
  },

  SET_SECOND_LEVEL_STEPS(state, steps) {
    state.secondLevelSteps = steps;
  },

  SET_KNOWLEDGE_BASE(state, base) {
    state.knowledgeBase = base;
  },
};
